# git init ->превращает текушую директорию в git репозиторий (содается .git)
# git add .(.--> все файлы можно указать отдельные файлы) ->отслеживание всех файлов
# got commit -m "commit"->добавляет/совершает сохранение в git всех файлов которые находятся в индексе 
# git log -> выводит журнал(log) сохранений 
# git status -> показывет статус файлов
# gi remote add origin(название связи-обычно origin) SSH(ссылка на удаленый репозиторий)
# gti checkout hash_commit ->просматриваем версию
# git branch ->выводит список всех веток(звезлочкой помечена активная ветка)
# git checkout <название_ветки>
# 				- переход на указанную ветку
# git switch <название_ветки>
# git push origin(название_связи) master(название_ветки) -> отправка версий кода на удаленный  репозиторий
# git clone <ссылка_на_удаленный_репозиторий> -> склонировать/скачать репозиторий

# порядок git init | git add . | git commit -m 'comment' | git remote add origin <url> | git push origin master


# git add <название файла> | git commit -m <=> | git push origin master | -> изменение

# hello

# test
#.